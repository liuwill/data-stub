// Code generated by data-stub. DO NOT EDIT.
// source: <%= tableName %> [ <%= fileName %> <%= tableComment %>]

import BaseModel from '../model'

export interface I<%= baseClassName %>Define {
<% columns.forEach(function(item){ %>
  <%= item.field %>: <%= item.defineType %>
<% }); %>
}

const model = {
  table: '<%= tableName %>',
  comment: '<%= tableComment %>',
  name: '<%= fileName %>',
  fields: {
<% columns.forEach(function(item){ %>
    '<%= item.field %>': {
      type: <%= item.targetType %>,
      rawType: '<%= item.targetType %>',
      name: '<%= item.field %>',
      comment: '<%= item.comment %>',<% if (!item.nullMark) { %>
      allowNull: false,<% } %><% if (item.defaultValue) { %>
      defaultValue: '<%= item.defaultValue %>',<% } %>
    },
<% }); %>
  },
}
export default new BaseModel(model)
